{
  "name": "@interview-prep-kit/frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "echo 'Next.js app scaffolded in milestone M9' && exit 0",
    "build": "echo 'Next.js app scaffolded in milestone M9' && exit 0",
    "test": "echo 'no tests yet' && exit 0",
    "lint": "echo 'no lint config yet' && exit 0"
  }
}
